/* global define */
'use strict';

define(['mindplot', 'utils', 'web2d', 'polyfills'], (Mindplot, Utils, Web2D) => {
    return {
        'Mindplot': Mindplot,
        'Utils': Utils,
        'Web2D': Web2D
    };
});
